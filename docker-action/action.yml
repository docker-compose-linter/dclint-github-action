name: DCLint Docker Action
description: Lints Docker Compose files using dclint Docker image
author: Sergey Kupletsky <s.kupletsky@gmail.com>
branding:
  icon: check
  color: blue

inputs:
  args:
    description: CLI arguments to pass to DCLint
    required: false
    default: '["."]'

runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ fromJson(inputs.args) }}
